
usage: predict.py [-h] -m MDIR -t TMPDB [-n NTMP] [--windowed] [--one-shot]
                  MSA TAPE HHR NPZ

positional arguments:
  MSA         input multiple sequence alignment in A3M/FASTA format
  TAPE        TAPE features
  HHR         HHsearch hits
  NPZ         predicted distograms and anglegrams

optional arguments:
  -h, --help  show this help message and exit
  -m MDIR     folder with the pre-trained network (default: None)
  -t TMPDB    FFINDEX DB with templates (default: None)
  -n NTMP     number of top HHsearch hits to use (default: 25)
  --windowed  perform windowed predictions (takes longer) (default: True)
  --one-shot


Example:

python ./predict.py -m ./network00 -t /projects/ml/TrRosetta/pdb100_2020Mar11/pdb100_2020Mar11 -n 25 \
                    example/fake.a3m example/fake.tape.npy example/fake.hhr example/fake.tbm.npz
